datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Merchant {
  id            String   @id
  name          String
  location      String
  registeredAt  DateTime @default(now())
  score         Int      @default(0)
  scoreBreakdown Json
  stats         Json
}